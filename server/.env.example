# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Database Configuration
DATABASE_URL="postgresql://imageai:imageai_pass@localhost:55432/imageai_db?schema=public"

# Server Configuration
PORT=8080
NODE_ENV=development

# CORS Configuration (comma-separated origins)
CORS_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"

# Development Authentication (for local testing only)
# Set to 1 to enable DevAuth (bypasses Firebase token verification)
# WARNING: Never enable this in production!
DEV_AUTH_ENABLED=0
DEV_AUTH_TOKEN=dev

# Firebase Admin SDK Credentials
# Option 1: Use firebase-adminsdk.json file (recommended for local dev)
# Place firebase-adminsdk.json in server root directory

# Logging Configuration
# LOG_LEVEL=debug  # Options: error, warn, log, debug, verbose
# Default: debug in development, warn in production

# Admin Emails (comma-separated, for future admin whitelist feature)
# ADMIN_EMAILS=admin@example.com,manager@example.com
